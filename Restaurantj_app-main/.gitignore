اريد القيام كل خطوة على  حدا اي بعد الانتهاء من خطوة واتاكد من عدم وجود مشكلة نذهب للخطة التالية
🧩 المرحلة 1 — (أنت فيها الآن ✅) 

الهدف: ربط Flask بقاعدة بيانات SQLite باستعمال SQLAlchemy

✅ إنشاء قاعدة البيانات restaurant.db

✅ إنشاء الجداول User, Plat

✅ عرض الأطباق في صفحة “Plats” من قاعدة البيانات

✅ إدخال بيانات أولية عبر seed.py

📍 أنت في نهاية هذه المرحلة الآن.

🧩 المرحلة 2 — إنشاء واجهة لإضافة البيانات من الموقع (Formulaire HTML)

الهدف:
بدل أن تضيف الأطباق يدويًا بـ seed.py،
سنبني صفحة اسمها مثلًا “/ajouter-plat” فيها نموذج HTML يسمح لك بإضافة:

اسم الطبق

السعر

الفئة

رابط الصورة

وسنربطها بـ Flask لإدخال البيانات في قاعدة البيانات أوتوماتيكيًا.

📘 تعلم جديد:

استخدام request.form في Flask

إرسال بيانات POST

التعامل مع db.session.add()

🧩 المرحلة 3 — إدارة السلة والطلبات (Panier & Commande)

الهدف:
ننشئ جدولين جديدين:

Panier 🛒

Commande 📦

وسنجعل المستخدم يضيف الأطباق إلى السلة، ثم يُرسل الطلب لتُخزّن البيانات في قاعدة البيانات.

📘 تعلم جديد:

العلاقات بين الجداول (Foreign Keys)

عمليات CRUD (Insert / Delete / Update / Read)

🧩 المرحلة 4 — نظام المستخدمين (Login / Register)

الهدف:
ننشئ نظام دخول وتسجيل بسيط يسمح لكل مستخدم أن:

يسجل حساب جديد

يسجل الدخول

يرى طلباته الخاصة فقط

📘 تعلم جديد:

استخدام Flask-WTF أو Flask-Login

حفظ الجلسات (sessions)

التحقق من كلمات المرور

🧩 المرحلة 5 — تحسين التصميم واحتراف الواجهة

الهدف:
تحسين واجهة الموقع:

استعمال Bootstrap أو TailwindCSS

جعل الموقع متجاوبًا (Responsive)

توحيد التصميم عبر base.html

📘 تعلم جديد:

القوالب الموروثة (template inheritance)

تصميم واجهات احترافية

🧩 المرحلة 6 — رفع المشروع على GitLab (TP prêt à rendre)

الهدف:
رفع المشروع النهائي على GitLab:

إعداد README.md وrequirements.txt

إنشاء dépôt جديد

رفع المشروع بالأوامر:

git init
git add .
git commit -m "Version finale du TP"
git remote add origin <URL_GitLab>
git push -u origin main

🚀 النتيجة النهائية

بعد هذا المسار، ستكون قادرًا على بناء تطبيق Flask متكامل:

فيه قاعدة بيانات

فيه CRUD كامل (إضافة / تعديل / حذف / عرض)

فيه تسجيل دخول

فيه تصميم احترافي وجاهز للتسليم أو التطوير الشخصي